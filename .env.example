# PixOrder Environment Configuration
# Copy this file to .env and fill in your values

# Code Signing Identity (optional but recommended)
# To find your identity, run: security find-identity -p codesigning -v
# Example: CODE_SIGN_IDENTITY="Developer ID Application: Your Name (TEAM_ID)"
# Example: CODE_SIGN_IDENTITY="Apple Development: your.email@example.com (TEAM_ID)"
CODE_SIGN_IDENTITY=""

# If you don't set CODE_SIGN_IDENTITY, the app will be unsigned
# Users will see security warnings when first opening the app
# They can bypass this by right-clicking and selecting "Open"